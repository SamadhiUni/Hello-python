The github repository purpose is essentially a strong space for your code and related files.think of it as a folder where you can keep,organize and share your projects.its purpose goes beyond just storying files.

The python script is simplify a file containing PYTHON CODE that the computer can execute.its purpose depend on what the code inside does.
